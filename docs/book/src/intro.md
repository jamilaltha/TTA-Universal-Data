# Introduction

ADN-Chain v1 is a codon-inspired blockchain runtime that combines a 64-bit instruction set (ISA-64) with a deterministic virtual machine. The system targets reproducible on-chain computation, predictable resource metering, and modular consensus. This book summarizes the architecture and serves as a companion to the LaTeX whitepaper.
